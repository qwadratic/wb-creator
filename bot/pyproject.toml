[project]
name = "wb-bot"
version = "0.1.0"
description = "Ortobor Workbook Telegram bot"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot[ext]==21.6",
    "httpx==0.27.2",
    "python-dotenv==1.0.1",
    "python-pptx==1.0.2",
    "pypdf==5.1.0",
]

[tool.uv]
package = false
